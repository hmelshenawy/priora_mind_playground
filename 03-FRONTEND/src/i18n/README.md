# i18n (next-intl)

Bilingual message catalogs for Arabic (RTL) and English (LTR) — Constitution X.

> **Setup phase**: only placeholder catalogs exist. The Foundational phase
> (task T013) wires `next-intl` — the plugin in `next.config.mjs`, the request
> config, the routing/middleware, and dynamic `lang`/`dir` in `layout.tsx`.
> Per-story catalogs (onboarding, assessment, safety) are added in US1–US9.

Locales: `en` (default, LTR), `ar` (RTL). See `src/i18n/messages/`.